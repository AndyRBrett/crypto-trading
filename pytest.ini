[pytest]
# Only collect the actual test suite. scripts/test_push.py matches pytest's
# default test_*.py glob but is a standalone CLI (it calls sys.exit at import),
# so a bare `pytest` from the repo root must not try to collect it.
testpaths = tests
